<?php 
    $id = '';
    
    if (!empty($this->zoneconfig['conf']['id'])){
        $id = ' id="'.$this->zoneconfig['conf']['id'] .'"';
    }
    
    $name = '';
    if (!empty($this->zoneconfig['conf']['name'])){
        $name = $this->zoneconfig['conf']['name'];
    }
    
    $data_melisKey = 'data-melisKey="'.$this->melisKey.'"';
?>

<div class="me-heading bg-white border-bottom" <?php echo $id.' '.$data_melisKey; ?>>
	<div class="row">
		<div class="me-hl col-12 col-md-9">
			<h1 class="content-heading"><?php echo $this->translate($name) ?></h1>
			<p><?= $this->translate('tr_meliscore_tool_emails_mngt_header_description'); ?></p>
		</div>
  		<div class="me-hr col-12 col-md-3">
  			<?php 
            	if(!empty($this->zoneconfig['interface'])) {
            	    foreach($this->zoneconfig['interface'] as $keyConfig => $menuConfig) {
            	        echo $this->$keyConfig;
            	    }
            	}
            ?>
  		</div>	
	</div> 
</div> 
